# notepad4-mac 开发记录

写于 2026-09-17。只记本仓库 `/Users/limin/Documents/ceshi/notepad4-mac`，不要改 Windows 的 `ceshi/notepad4`。

## 现在到哪了

安装包是 **0.1.21**，已替换 `/Applications/Notepad.app`（二进制时间 2026-09-17 02:06）。打开 `CONTRIBUTING.md` 能画出来，启动后约 2 秒的 `PaintText` 空指针已经堵上；用了几分钟后的窗口动画释放崩溃（02:04）也堵上了。预览、大纲、百分比滚动、本地公式/Mermaid、可选文件树、导出，代码都在，但**还没提交**。

本地 `main` 比 `origin/main` 超 1 个提交（`a9e5ccc`，本地 KaTeX/Mermaid + 导出 + 文件树）。其后的预览改版、配色、两拨崩溃修复都还在工作区。没推远端。

## 这轮实际做了什么

目标从「HTML/MD 预览能用」收成三件事：预览行为按你的要求改、编辑器别再一打开就崩、验证由我自己开 App，不要再让你点。

已落地：

- 预览库进 bundle，`file://` 本地加载 KaTeX / Mermaid，不再走 CDN。
- 左侧文件树可开关，点文件打开。
- MD 大纲固定在预览左侧、宽 112px、跟着当前节、可点开关。
- 编辑器 ↔ 预览按**文档百分比**滚，不再按行/像素硬映射。
- 暗色代码块改成深底浅字（预览和编辑器一边），不再白字灰底。
- Markdown 标题不再写死 `size:`。混字号一折行就会把 Scintilla 画崩。
- `EditView::PaintText`：行号为负或 `LineLayout` 为空就跳过，不再写 `containsCaret`（崩溃地址 `0x2d`）。
- 窗口：`releasedWhenClosed = NO`，关掉系统窗口变形动画，启动时不因「还没 visible」再新开一扇。
- 审计加了 `--paintcrash` / `--wincrash`，强制走 `SCIContentView drawRect`。无头 `--audit` 最后一次是 **682 pass / 0 fail / 9 skip**。
- 审计误写入的 `NP4WrapColumn=12` 已改回 **80**。

## 怎么走到这一步

1. **预览**：HTML 要能看、MD 要公式和 Mermaid、不要 AI 排版。先做成右侧 WKWebView，导出图/PDF/Word 也接了。
2. **走错仓库**：改过 Windows `notepad4`，已停。后面只动 `-mac`。
3. **本地库**：`loadHTMLString` 拦本地脚本，公式/图一度是原文。改成 bundle 资源 + 临时目录 `loadFileURL`。
4. **你盯界面**：大纲要固定且窄、可开关；滚动按百分比；你问「预览不是只给网页的吗」。预览仍给 MD（公式/图），源码还在左边。
5. **「好丑」+ 打开即崩**：标题加大字号后第一次画编辑器就 SIGSEGV。无头测试全绿，因为 layer-backed 的 `drawRect` 根本没走。
6. **PaintText**：`lineDocPrevious` 初值是 `-1`，脏区 top 为负时 `DocFromDisplay` 也是 `-1`，跳过取行布局，对空指针写 `containsCaret`。01:41、01:45 都是这条。
7. **02:04 另一条**：进程活了约 3.5 分钟，死在 `_NSWindowTransformAnimation dealloc`。启动会误开第二扇「无标题」，切桌面/关窗时动画释放踩空。
8. **打开方式**：`--quiet --shot` 能证明能画，但窗口不在你桌面。后来用 `open -a` 开在当前桌面。02:04 那份崩溃报告是我从 `~/Library/Logs/DiagnosticReports/` 自己翻的。

## 心得（下次先按这个来）

- 无头 `--audit` 只能证明逻辑，不能证明第一次真正上屏。编辑器绘制必须 `SCIContentView display` / 负脏区；窗口生命周期必须真的 `close` 再 `CATransaction flush`。
- 崩溃先读本机 ips，不要等你粘贴。`far=0x2d` 对得上结构体偏移，比猜「折行/主题」快。
- 审计不要把 `NP4WrapColumn` 之类写进用户偏好后不收回。列宽 12 是测试留下的，不是你设的。
- Scintilla 这棵树的缩放是**百分比**（默认 100），不是点数偏移。`SCI_SETZOOM 100` 是复位，不是「缩到 100 磅」。
- Cocoa 上 `bufferedDraw` 是关的。`releasedWhenClosed = YES` 加系统窗口动画，在 macOS 26 上会在 `objc_release` 里炸。
- 工作只在 `notepad4-mac`。Windows 仓库和 Mac 壳不是同一份代码。

## 还缺什么

按会不会挡你用、排：

1. **工作区没提交**。预览改版、配色、两拨崩溃修复都未进 git。提交前要你点头；不要推远端，除非你说推。
2. **合并行**。光标在空行上的「合并行」看起来像删空行。还没改产品行为，只解释过。
3. **编辑器里的 MD 观感**。标题加大字号已撤回（会崩）。现在比改之前醒目一些，但不是「编辑器里直接所见即所得」。预览仍是渲染层。
4. **「好丑」还没做完**。大纲和滚动按你的规则改了，整体观感没有重做一版。
5. **仍可能有没踩到的崩**。启动画字和关窗动画两条封了。长时间编辑、多窗、大文件折行、折叠，没有同等的实机覆盖。
6. **打开文件仍可能再拉一进程**。当前标签不可复用时，`openURLs` 会 `createsNewApplicationInstance`。02:04 的两扇窗主要是「未 visible 再 new」，这条实例策略还在。
7. **控制台两句垃圾**：`Wait cursor is invalid` / `Reverse arrow cursor is invalid`。不影响用，没清。
8. **产品外壳**：仍无公证；Intel 要自己编；版本号还停在 0.1.21，和线上 DMG  dig 不出来这次修复。
9. **明确不做**：AI 排版。MD 保持源码 + 只读渲染；HTML 才回写。

## 本机对照

| 项 | 值 |
|---|---|
| 仓库 | `/Users/limin/Documents/ceshi/notepad4-mac` |
| 安装 | `/Applications/Notepad.app` |
| bundle | `com.limin640.notepad4mac` |
| 最近安装包 | 2026-09-17 02:06 |
| 最近崩溃 | `Notepad-2026-09-17-020454.ips`（窗口动画，已按这个修过） |
| 用户偏好（修完后） | 折行开、列宽 80、缩放 104% |
| 截图 | `实测-20260916/视觉/preview-fix/`、`实测-20260917/视觉/open-crash/` |
| 编译 | `DEVELOPER_DIR=/Library/Developer/CommandLineTools cmake --build build --target Notepad4` |
| 绘制复现 | `NP4_HEADLESS=1 ./build/Notepad.app/Contents/MacOS/Notepad --headless --quiet --paintcrash` |
| 关窗复现 | 同上，把 `--paintcrash` 换成 `--wincrash` |

下一件该做的：你点头就提交工作区（不含 `实测-*` 截图，除非你要一起进库），然后按你的用法再盯一次多标签和关窗。
